# Cuckoo Frontend

Next.js + React + TypeScript frontend for the ecommerce image translation workflow.

## Run

```powershell
cd frontend
copy .env.local.example .env.local
npm install
npm run dev
```

Open `http://127.0.0.1:3000`.
